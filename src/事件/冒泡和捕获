冒泡：当一个事件发生在一个元素上，它会首先运行在该元素上的处理程序，
然后运行其父元素上的处理程序，然后一直向上到其他祖先上的处理程序

event.target -- 引发事件的目标元素，在整个冒泡过程中不会发生变化
this(event.currentTarget) -- 当前正在处理的元素

因为适用于捕获和冒泡阶段
event.stopPropagation() 停止(单个)事件传播

当一个事件上有多个处理程序的时候，需要使用event.stopImmediatePropagation()


DOM事件传播的三个阶段：
捕获阶段 - 事件向下抵达目标元素
目标阶段 - 事件到达目标元素(捕获和冒泡都会触发)
冒泡阶段 - 事件从目标元素开始冒泡

